## A simple address retrieval.


Simply clone and install the packages and run!

Remember to first put your API key in `utils/fetchComponent.js` from [getAddress](https://getaddress.io/)
```
$ npm i
$ npm start
```